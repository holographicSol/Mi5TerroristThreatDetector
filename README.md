# Mi5TerroristThreatDetector

A simple ESP32 project to display terrorist threat level according to Mi5, for helping to monitor the situation.

Set AP Serial Command:
connect -ssid your_ssid -p your_password

AP is stored in SPIFFS and is loaded automatically on startup.
